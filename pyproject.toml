[project]
name = "li_4yp"
version = "0.1.1"
description = "A Python project"
authors = [
    { name="Boting Li", email="your.email@example.com" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
